-- โน้ตภายในทีม (Internal Note) ของโปรเจกต์ — รายละเอียดที่คุยกับลูกค้ามา
-- แต่ยังไม่อยากให้ลูกค้า/คนสมัครเห็น (เช่น เรทที่ต่อรองได้ ข้อจำกัดของลูกค้า
-- ชื่อคนติดต่อ เงื่อนไขที่ยังไม่ fix)
--
-- ⚠️ ห้ามนำ column นี้ไป render ในหน้าสาธารณะเด็ดขาด:
--    /casting/[id] (คนสมัคร) · /p/[token] (ลูกค้า) · print · report
--    ทั้งสองหน้านั้น select เฉพาะ column ที่ต้องใช้อยู่แล้ว (ไม่ใช้ *)
alter table projects
  add column if not exists internal_note text;
